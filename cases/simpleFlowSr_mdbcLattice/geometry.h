#include "SPH_defs.h"
#include "SPH_particle_system.h"

		Create_Wall_x_lattice(particles, 0. - dp*2, 2. + dp*2, 0. , -1);
		DRAW_CreateInletY(particles, 0.1  + 4*dp, 0.0 + dp, 0.2, -1, 4, {2.0, 0.}, inlet);
